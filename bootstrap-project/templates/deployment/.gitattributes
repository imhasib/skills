# Env files MUST be LF — CRLF breaks docker-compose `source` and embeds \r into values.
*.env text eol=lf
**/env/*.env text eol=lf
**/env.example text eol=lf
**/*.example text eol=lf

# Shell scripts
*.sh text eol=lf

# Nginx configs
*.conf text eol=lf
